USB stick with AVR ATMega32u2 microcontroller

PCB designed in KiCad.
The code is for emulating a keyboard and typing a password from EEPROM when the button is pressed.
It uses the LUFA usb library. 
